<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Theme for Compose Activity -->
    <style name="Theme.WhatsUpDog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.WhatsUpDog.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_dog</item>
        <item name="postSplashScreenTheme">@style/Theme.WhatsUpDog</item>
    </style>
</resources>